# APPLICATION
spring.application.name=evotemali-backend
server.port=8080
spring.main.allow-bean-definition-overriding=true

# DATABASE
spring.datasource.url=jdbc:mysql://localhost:3306/evote_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=evoteuser
spring.datasource.password=motdepasse

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# CORS SIMPLE (sans configuration Java)
# Les contrôleurs utilisent déjà @CrossOrigin